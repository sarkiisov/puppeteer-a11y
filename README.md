# Puppeteer a11y

Инструмент автоматической проверки веб-страниц на соответствие требованиям доступности по стандарту WCAG 2.1.

## Технологический стек

- TypeScript
- Puppeteer Core
- Commander
- Zod
- Franc
- Langs
- Vitest
- Python (Pandas, Matplotlib, ipywidgets, Jupyter Lab) — пример аналитики.

## Проверки и результаты

Инструмент включает набор проверок веб-страниц, каждая из которых соответствует определённому критерию WCAG. Реализация проверок с тестами содержится в директории `src/checks`. Реализованные проверки:

- [1.1.1 Non-text Content](https://www.w3.org/WAI/WCAG21/quickref/#non-text-content)
- [1.2.2 Captions (Prerecorded)](https://www.w3.org/WAI/WCAG21/quickref/#captions-prerecorded)
- [1.4.12 Text Spacing](https://www.w3.org/WAI/WCAG21/quickref/#text-spacing)
- [1.4.3 Contrast (Minimum)](https://www.w3.org/WAI/WCAG21/quickref/#contrast-minimum)
- [1.4.4 Resize Text](https://www.w3.org/WAI/WCAG21/quickref/#resize-text)
- [2.4.2 Page Titled](https://www.w3.org/WAI/WCAG21/quickref/#page-titled)
- [2.4.7 Focus Visible](https://www.w3.org/WAI/WCAG21/quickref/#focus-visible)
- [3.1.1 Language of Page](https://www.w3.org/WAI/WCAG21/quickref/#language-of-page)
- [3.1.2 Language of Parts](https://www.w3.org/WAI/WCAG21/quickref/#language-of-parts)

Структура результатов проверки:

```ts
export interface PageCheckResult {
  id: string
  name: string
  level: 'A' | 'AA' | 'AAA'
  result: {
    status: 'PASSED' | 'FAILED' | 'REVIEW_NEEDED' | 'ERROR'
    details?: any
    recommendations?: string
  }
}

export interface RunPageChecksResult {
  meta: {
    url: string
    timestamp: Date
  }
  results: PageCheckResult[]
}
```

`details` — содержит дополнительные сведения о результатах проверки. В зависимости от конкретного критерия оно может включать текстовое пояснение, объект с информацией о проблемных элементах страницы (например, для последующей локализации), либо другую полезную информацию, необходимую для анализа и устранения нарушений

`status` — отражает итоговый результат выполнения проверки и может принимать следующее значение:

- PASSED — проверка пройдена, нарушений не обнаружено;
- FAILED — проверка не пройдена, обнаружены нарушения;
- REVIEW_NEEDED — автоматическая проверка не может однозначно определить результат, требуется ручная оценка;
- ERROR — при выполнении проверки произошла ошибка, результат недоступен

`recommendations` — предоставляет конкретные рекомендации по исправлению выявленных нарушений

## Переменные окружения

| Переменная                  | Описание                                    | По умолчанию |
| --------------------------- | ------------------------------------------- | ------------ |
| `PUPPETEER_EXECUTABLE_PATH` | Путь к Chrome / Chromium                    | не задан     |
| `PUPPETEER_HEADLESS`        | Запуск в headless-режиме (`true` / `false`) | `true`       |
| `PUPPETEER_TIMEOUT_MS`      | Таймаут запуска браузера (мс)               | `30000`      |
| `PUPPETEER_WIDTH`           | Ширина viewport браузера                    | `800`        |
| `PUPPETEER_HEIGHT`          | Высота viewport браузера                    | `600`        |

## Локальный запуск

### Установка зависимостей

```
corepack enable pnpm
pnpm install
```

### Запуск проверок

```
pnpm run dev run https://example.com
```

## Docker

### Сборка Docker-образа локально

```bash
docker build -t sarkiisov/puppeteer-a11y .
```

## Загрузка готового образа из Docker Hub

```
docker pull sarkiisov/puppeteer-a11y:latest
```

### Запуск контейнера в фоновом режиме

```
docker run -d --name puppeteer-a11y-container sarkiisov/puppeteer-a11y
```

### Запуск проверок внутри контейнера

```
# pa11y — алиас для node /app/dist/index.js

# Проверка публичного сайта
docker exec puppeteer-a11y-container pa11y run https://example.com

# Проверка локального приложения, запущенного на хост-машине
docker exec puppeteer-a11y-container pa11y run https://host.docker.internal:3000
```

## Перенаправление результатов вывода

Результат выполнения проверок можно перенаправлять в файл для последующего анализа или сохранения отчетов

### 🪟 Особенности работы в Windows

Для предотвращения проблем с кодировкой в создаваемом отчете рекомендуется выполнять команды в терминале Git Bash, а не из стандартного cmd или PowerShell.

```
# флаг --silent исключает служебные логи pnpm
pnpm run --silent dev run http://localhost:3000 > output.json

docker exec puppeteer-a11y-container pa11y run https://host.docker.internal:3000
```

## Пример аналитики с Python

Результаты работы инструмента формируются в формате JSON, что позволяет использовать их для дальнейшего анализа и обработки с помощью любых удобных инструментов и технологий. Полученные данные могут быть загружены в аналитические среды, такие как Jupyter Lab, либо интегрированы в существующие процессы отчетности и автоматизации.

### Переход в директорию с примером

```
cd analytics
```

### Создание и активация виртуального окружения

```
# Windows
python -m venv venv
.\venv\Scripts\activate

# Linux / macOS
python3 -m venv venv
source venv/bin/activate
```

### Установка зависимостей

```
pip install -r requirements.txt
```

### Запуск Jupyter Lab

```
jupyter lab
```
